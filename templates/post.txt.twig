{{-post.build()-}}
{%- if not post.nameIsId or post.imagePath is not empty or postsCount == 0 -%}
{%-	if not post.nameIsId or postsCount == 0 -%}
{%-		if postsCount == 0 -%}
{{post.name}}
====================
{%		else %}
{{post.name}}
----------
{%		endif %}

{%	endif %}
{% endif %}
{{post.date|date(dateFormat)}} {##}
{%- if post.nameIsId and postsCount > 0 %}

----------

{% endif %}
{%- if post.terms is defined %}in {% for tag in post.terms %}{{tag.name}} {% endfor %}
{% endif %}
{% if postsCount == 0 and post.imagePath is not empty %}


![{{post.imageAlt ?? 'image'}}]({{post.imagePath}})
{%- endif %}


{% if postsCount > 0 %}
{{post.getExcerpt(postsCount > 20)|trim|raw}}

<{{post.url}}>

--------------------------------------------

{% else %}
{{post.content|trim|raw}}
{% endif %}
